import { Container, Typography } from "@mui/material";

const Organization = () => {
  return (
    <Container>
      <Typography variant="h4">Manage Organization</Typography>
      {/* Add logic to manage organization */}
    </Container>
  );
};

export default Organization;
